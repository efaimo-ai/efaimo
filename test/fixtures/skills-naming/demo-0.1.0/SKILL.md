---
name: demo
description: A demonstration skill for validating archive-style directory naming. Use when testing how efaimo treats an unpacked release tarball whose directory carries a version suffix.
---

# demo

This fixture simulates a skill unpacked from a release tarball, where the
directory is named `demo-0.1.0` while the skill itself is named `demo`.
